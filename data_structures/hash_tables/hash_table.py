# Time complexities for:
# Access: Not applicable (since we cannot access to indices)
# Search: O(1) (Worst case - O(n))
# Insertion: O(1) (Worst case - O(n))
# Deletion: O(1) (Worst case - O(n))

# To store and find the data it's using Hash Function

a_hash = {}
a_hash[1893] = "Year"
print(a_hash[1893])
